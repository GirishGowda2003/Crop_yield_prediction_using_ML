Go to the terminal and run all the required libraries: 
streamlit==1.29.0
pandas==2.1.3
numpy==1.26.2
scikit-learn==1.3.2
joblib==1.3.2
plotly==5.18.0
seaborn==0.13.0
matplotlib==3.8.2

Then go to the new terminal and run   python train_model.py to train the model
here u can see all the trained model:

model.pkl
district_encoder.pkl
crop_encoder.pkl
state_encoder.pkl
season_encoder.pkl
